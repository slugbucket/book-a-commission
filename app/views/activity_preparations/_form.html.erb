<%= form_for(activity_preparation) do |f| %>
  <% if activity_preparation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_preparation.errors.count, "error") %> prohibited this activity_preparation from being saved:</h2>

      <ul>
      <% activity_preparation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
