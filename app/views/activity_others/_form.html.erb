<%= form_for(activity_other) do |f| %>
  <% if activity_other.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_other.errors.count, "error") %> prohibited this activity_other from being saved:</h2>

      <ul>
      <% activity_other.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
