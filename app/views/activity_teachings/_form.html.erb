<%= form_for(activity_teaching) do |f| %>
  <% if activity_teaching.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_teaching.errors.count, "error") %> prohibited this activity_teaching from being saved:</h2>

      <ul>
      <% activity_teaching.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_name %>
    <%= f.text_field :student_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
