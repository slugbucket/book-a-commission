<%= form_for(activity_development) do |f| %>
  <% if activity_development.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_development.errors.count, "error") %> prohibited this activity_development from being saved:</h2>

      <ul>
      <% activity_development.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
