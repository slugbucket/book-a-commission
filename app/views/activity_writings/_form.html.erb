<%= form_for(activity_writing) do |f| %>
  <% if activity_writing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_writing.errors.count, "error") %> prohibited this activity_writing from being saved:</h2>

      <ul>
      <% activity_writing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
